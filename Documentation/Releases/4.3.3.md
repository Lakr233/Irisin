Irisin 4.3.3 publishes its debug symbols. Nothing in the app changed.

- `Irisin_4.3.3_dSYMs.zip` holds the dSYMs for the app, the daemon and the install helper of both packages. Use it to symbolicate crash reports from this version.

Choose `iphoneos-arm64e` for RootHide or `iphoneos-arm64` for rootless firmware. SHA-256 checksums are included in `SHA256SUMS`.
