Irisin 4.1.3 — installed packages keep their icon, name and depiction.

- A package this app installed keeps the icon, the name, the depiction and
  its header image the repository gave it, in the Installed list and on its
  page. The Installed list sorts and searches by the name it shows.
- On iPad the package lists no longer draw a frame of tiny cells at launch.

| Jailbreak | Package |
| --- | --- |
| roothide | `wiki.qaq.irisin_4.1.3_iphoneos-arm64e.deb` |
| rootless (`/var/jb`) | `wiki.qaq.irisin_4.1.3_iphoneos-arm64.deb` |

SHA-256 sums are in `SHA256SUMS`.
