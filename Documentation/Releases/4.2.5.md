Irisin 4.2.5 — a package page's banner and footer, tidied.

- The handwriting on a package page's banner travels with the banner as
  a photo resizes it, and the photo fades in with no blurred copy of
  itself behind it.
- The architecture under a package page is plain text again. One the
  device does not run is no longer marked letter by letter in red; the
  line under it still says when the package installs in compatibility
  mode.

| Jailbreak | Package |
| --- | --- |
| roothide | `wiki.qaq.irisin_4.2.5_iphoneos-arm64e.deb` |
| rootless (`/var/jb`) | `wiki.qaq.irisin_4.2.5_iphoneos-arm64.deb` |

SHA-256 sums are in `SHA256SUMS`.
