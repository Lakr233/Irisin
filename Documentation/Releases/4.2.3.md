Irisin 4.2.3 — a translated package page is as tall as its text.

- A package page translated from its menu takes the height of what it
  now says. Compared, which puts the translation under each piece, used to
  keep the height of the page as written and cut the rest off.

| Jailbreak | Package |
| --- | --- |
| roothide | `wiki.qaq.irisin_4.2.3_iphoneos-arm64e.deb` |
| rootless (`/var/jb`) | `wiki.qaq.irisin_4.2.3_iphoneos-arm64.deb` |

SHA-256 sums are in `SHA256SUMS`.
